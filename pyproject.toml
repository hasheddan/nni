[project]
name = "nni"
version = "0.1.0"
description = "Nearest Neighbor Interpolation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "cocotb>=2.0.1",
    "imageio>=2.37.3",
    "pytest>=9.1.1",
    "rawpy>=0.27.0",
]
